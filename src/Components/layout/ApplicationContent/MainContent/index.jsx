import styled from 'styled-components';
// eslint-disable-next-line import/named
import { MainContentStyles } from './style';

const MainContent = styled.main`${MainContentStyles}`;

export default MainContent;
