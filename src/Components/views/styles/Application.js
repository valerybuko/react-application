import { css } from 'styled-components';
import { backgroundImage } from '../../../mockdata/imagesURL';

export const ApplicationStyles = css`
    width: 100%;
    background-image: url(${backgroundImage});
`;
